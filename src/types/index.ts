export interface Artigo {
  id: number;
  date: string;
  title: string;
  excerpt: string;
}

export interface AreaAtuacao {
  id: number;
  title: string;
  description: string;
}

export interface Diferencial {
  id: number;
  title: string;
  description: string;
}
